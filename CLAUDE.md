# CLAUDE.md

このファイルは、Claude Code (claude.ai/code) がこのリポジトリで作業する際のガイダンスを提供します。

## アプリの起動

```bash
streamlit run app.py
```

アプリは `http://localhost:8501` で起動します。初回起動時にメールアドレスの入力を求められる場合は、`~/.streamlit/credentials.toml` を以下の内容で作成してください：

```toml
[general]
email = ""
```

または `--server.headless true` オプションを付けて起動するとスキップできます。

## 環境セットアップ

`.env.example` を `.env` にコピーして `GEMINI_API_KEY` を設定してください。APIキーはサイドバーからも実行時に入力できます（再起動不要）。

```bash
pip install -r requirements.txt
```

## アーキテクチャ

アプリ全体が `app.py` の単一ファイルで構成されています。モジュール分割・pagesディレクトリ・外部ヘルパーは存在しません。

**各タブの処理フロー：**
1. ユーザーがStreamlitウィジェット（テキスト入力・セレクトボックス等）を操作
2. ボタン押下時、入力値をf文字列に埋め込んでプロンプトを構築
3. `generate(prompt)` が `genai.GenerativeModel(model_name).generate_content(prompt)` を呼び出し `response.text` を返す
4. `result_area(content, filename)` が結果をMarkdownでレンダリングし、ダウンロードボタンを表示

**主要なグローバル変数（サイドバーで設定、`generate()` 内で参照）：**
- `api_key` — `.env` またはサイドバー入力から取得。変更時に `genai.configure()` へ渡される
- `model_name` — サイドバーで選択。`generate()` 呼び出しごとに新しい `GenerativeModel` インスタンスが生成される

**各タブとプロンプト：**

| タブ | 主な入力 | プロンプトのペルソナ |
|---|---|---|
| ブログ記事 | テーマ・アウトライン・文体・対象読者・長さ・言語 | プロのブログライター |
| メール返信 | 元メール・返信意図・文体・名前・言語 | プロのビジネスライター |
| 文章要約 | テキスト・形式・詳細度・重視点・言語 | なし |
| SNS投稿 | テーマ・プラットフォーム・トーン・ハッシュタグ・言語 | SNSマーケティングの専門家 |
| 文章校正 | テキスト・校正観点・出力スタイル・言語 | なし |
| タイトル生成 | 内容概要・スタイル・生成数・種類・言語 | コピーライティングの専門家 |

すべてのプロンプトは各タブのボタンハンドラ内にインラインのf文字列として記述されており、プロンプトテンプレートの外部ファイルは存在しません。

## 新しいタブを追加する手順

1. `st.tabs([...])` の引数にラベルを追加し、新しい変数をアンパック
2. 既存のパターンに倣って `with tabN:` ブロックを記述（ウィジェット → ボタン → プロンプトf文字列 → `generate()` → `result_area()`）
3. Streamlitのウィジェット競合を避けるため、各ボタンに一意の `key=` 引数を設定する

## プロジェクトの方針

- **個人用ツール**のため、認証・データベース・ユーザー管理は追加しない
- 機能追加はすべてタブとして `app.py` に追記する（ファイル分割しない）
- プロンプトは日本語で記述する（出力言語はユーザーが選択できる）

## Gemini API の注意点

- モデルは毎回 `generate()` 内でインスタンス化する（グローバルに保持しない）
- ストリーミングは使用しない（`generate_content()` の同期呼び出しのみ）
- APIキーは `.env` の `GEMINI_API_KEY` から読み込む。`.env` はコミットしない（`.env.example` のみ管理する）
